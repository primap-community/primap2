* Add `publication_date` property for datasets to record the date of publication, which
  is especially useful for datasets which are published continuously and import into
  PRIMAP only occasionally.
