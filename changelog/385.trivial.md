* Switched the development setup and the packaging to [`uv`](https://docs.astral.sh/uv/).
  All packaging metadata moved from `setup.cfg` to `pyproject.toml`, the versions used
  for development are now pinned in `uv.lock`, and the development environment lives in
  `.venv/` instead of `venv/`.
