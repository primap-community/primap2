`ds.pr.ensure_valid()` now raises an error if a single gas is contained in a dataset
more than once, either as a mass and as a global warming potential, or in two different
global warming potential metrics.
