* {py:meth}`xarray.Dataset.pr.coverage` now works on datasets which carry processing
  information. Processing information variables are not data and are therefore never
  counted, matching the behaviour of {py:meth}`xarray.Dataset.pr.to_df`. Previously, the
  call failed with a `NotImplementedError` raised deep inside
  {py:meth}`xarray.Dataset.pr.sum`.
